[connections.supabase]
url = "https://sua-url.supabase.co"
key = "sua-anon-key"
```

Substitui pelos valores que você copiou no passo 1.

**3. Atualiza o `requirements.txt`** — adiciona essa linha se ainda não tiver:
```
st-supabase-connection
```

**4. Deploy** — substitui o `main.py` pelo que entregue anteriormente e faz o deploy no Streamlit Cloud.

A estrutura da sua pasta deve estar assim:
```
seu-projeto/
├── .streamlit/
│   └── secrets.toml
├── main.py
└── requirements.txt
